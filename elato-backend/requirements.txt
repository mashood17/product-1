fastapi>=0.115
uvicorn[standard]>=0.30
pydantic-settings>=2.5
python-dotenv>=1.0
pyjwt>=2.9
argon2-cffi>=23.1
httpx>=0.27
supabase>=2.9
email-validator>=2.2
python-multipart>=0.0.9
Pillow>=10.4
pillow-avif-plugin>=1.4
# Hero video metadata probing + poster-frame extraction (app/services/hero_video_service.py) —
# ships its own bundled decoder libraries, so no system ffmpeg install is required.
av>=13.0
# Hero video transcoding, used only for sources that aren't already H.264 MP4
# (app/services/hero_video_service.py's `_needs_transcode`) — ships a static,
# pre-built ffmpeg binary per-platform inside the wheel itself, so `pip install`
# alone is enough; no `apt install ffmpeg` / system package / Dockerfile change
# needed, which matters on Render's native Python runtime (no root/apt access).
imageio-ffmpeg>=0.5
